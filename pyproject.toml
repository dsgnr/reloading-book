[project]
name = "reloading-book"
version = "0.1.0"
description = "Notes and documentation for F-class reloading"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
docs = [
    "zensical>=0.0.50",
]
